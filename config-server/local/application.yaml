spring:
  cloud:
    consul:
      discovery:
        health-check-url: http://localhost:8888/actuator/health # 健康檢查 URL
        ip-address: 127.0.0.1 # 本地環境下的 IP 地址
        hostname: localhost # 本地環境下的主機名